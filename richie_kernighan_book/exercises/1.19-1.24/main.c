#include <stdio.h>
#include "1_19.c"
#include "1_20.c"
#include "1_22.c"
#include "1_23.c"

#define MAX_STRING 200

int main()
{
    deleteCommentsFromFile("1_19.c");

    // wordWrap(20);
    // detab();
    // char c;
    // char str[MAX_STRING];
    // int i;
    // i = 0;

    // while((c = getchar()) != '\n' && c != EOF)
    // {
    //     str[i] = c;
    //     i++;
    // }

    // reverse(str);
    // return 0;
}